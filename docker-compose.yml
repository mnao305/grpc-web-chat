version: '3'
services:
  proto:
    command: ./proto/protoc.sh
    build: ./proto
    volumes:
      - .:/proto
  server:
    build: ./server
    volumes:
      - ./server:/go/src/grpc-web-chat
    ports:
      - "9090:9090"
    depends_on:
      - proto
  envoy:
    build: ./proxy
    ports:
      - "8080:8080"
    links:
      - server